import{PageIntro}from"@/components/PageIntro";import{ActionForm}from"@/components/ActionForm";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <><PageIntro index={`PROOF / ${id} / PAYOUT`} title="Transfer the approved creator reward." copy="The percentage is computed by contract from the approved verdict, never supplied by the frontend."/><section className="form-section"><ActionForm mode="payout" id={id}/></section></>}
