import { PageIntro } from "@/components/PageIntro";import { Reveal } from "@/components/Reveal";
const steps=[["01","FUND","Brand names one creator and locks the entire GEN reward."],["02","LOCK RULES","Brief, disclosures, forbidden claims, score threshold, and deadline become immutable."],["03","PUBLISH","The assigned creator submits a public deliverable URL from their wallet."],["04","ADJUDICATE","GenLayer validators read the page and agree on the substantive escrow verdict."],["05","REVISE OR APPEAL","Fixable delivery receives revision; rejection retains one complete-record appeal."],["06","SETTLE","Approved funds transfer to the creator. Cancellable or final remainder returns to the brand."]];
export default function Page(){return <><PageIntro index="PROTOCOL / 00" title="From campaign brief to final transfer." copy="Every step changes one explicit contract state and preserves the evidence that justified it."/><section className="steps">{steps.map(([n,t,c],i)=><Reveal className="step" delay={i*.04} key={n}><b>{n}</b><h2>{t}</h2><p>{c}</p></Reveal>)}</section></>}
